services:
  health:
    build: .
    ports:
      - "8443:8443"
    volumes:
      - ./health.db:/app/health.db
      - ./logs:/app/logs
      - ./avatars:/app/static/avatars
    env_file:
      - .env
    restart: unless-stopped
